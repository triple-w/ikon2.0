<?php
declare(strict_types=1);require dirname(__DIR__).'/bootstrap.php';helper(['general','date_time','plugin','currency']);require_once APPPATH.'ThirdParty/PHP-Hooks/php-hooks.php';$db=require dirname(__DIR__).'/Increment02/isolated_database.php';$settings=[];foreach($db->table('settings')->get()->getResult()as$s)$settings[$s->setting_name]=$s->setting_value;config('Rise')->app_settings_array=$settings+['timezone'=>'UTC'];session();$p=0;$f=0;$a=static function(bool$ok,string$m)use(&$p,&$f){echo($ok?'[PASS] ':'[FAIL] ').$m.PHP_EOL;$ok?$p++:$f++;};$a($db->tableExists('sale_fiscal_pricing_preparations'),'Preparation snapshot table exists in isolated database.');$a($db->fieldExists('tax_pricing_mode','fiscal_profiles')&&$db->fieldExists('allow_sale_tax_pricing_override','fiscal_profiles'),'Issuer pricing policy columns exist.');
$company=$db->table('company')->where('deleted',0)->get(1)->getRow();$client=$db->table('clients')->where('deleted',0)->get(1)->getRow();$user=$db->table('users')->where('deleted',0)->get(1)->getRow();$regime=$db->table('sat_tax_regimes')->where('is_active',1)->get(1)->getRow();$now=date('Y-m-d H:i:s');$issuer=['profile_type'=>'issuer','company_id'=>$company->id,'rfc'=>'XAXX010101000','legal_name'=>'EMISOR PRICING AISLADO','tax_regime_id'=>$regime->id,'fiscal_postal_code'=>'06000','expedition_postal_code'=>'06000','fiscal_country_code'=>'MEX','status'=>'ready','is_default'=>1,'tax_pricing_mode'=>'tax_inclusive','allow_sale_tax_pricing_override'=>1,'created_at'=>$now,'updated_at'=>$now];$db->table('fiscal_profiles')->insert($issuer);$issuerId=(int)$db->insertID();
$sourceItem=$db->table('items')->where('deleted',0)->get(1)->getRowArray();unset($sourceItem['id']);$sourceItem['title']='Pricing isolated item';$sourceItem['description']='Exact fiscal simulation fixture';$sourceItem['rate']='100';$db->table('items')->insert($sourceItem);$itemId=(int)$db->insertID();$productKey=$db->table('sat_product_service_keys')->where('is_active',1)->get(1)->getRow();$unit=$db->table('sat_unit_keys')->where('is_active',1)->get(1)->getRow();$object=$db->table('sat_tax_object_codes')->where('code','02')->get(1)->getRow();$db->table('item_fiscal_settings')->insert(['item_id'=>$itemId,'item_type'=>'product','sat_product_service_key_id'=>$productKey->id,'sat_unit_key_id'=>$unit->id,'tax_object_code_id'=>$object->id,'is_default'=>1,'status'=>'ready','deleted'=>0,'created_at'=>$now,'updated_at'=>$now]);$settingId=(int)$db->insertID();$tax=$db->table('taxes')->where(['use_for_fiscal'=>1,'is_fiscal_ready'=>1,'xml_rate'=>'0.160000','fiscal_tax_type'=>'transfer','deleted'=>0])->get(1)->getRow();if(!$tax){$code=$db->table('sat_tax_codes')->where('code','002')->get(1)->getRow();$factor=$db->table('sat_tax_factor_types')->where('code','Tasa')->get(1)->getRow();$db->table('taxes')->insert(['title'=>'IVA 16 pricing fixture','percentage'=>'16','sat_tax_code_id'=>$code->id,'fiscal_tax_type'=>'transfer','factor_type_id'=>$factor->id,'xml_rate'=>'0.160000','is_fiscal_ready'=>1,'use_for_administrative'=>1,'use_for_fiscal'=>1,'deleted'=>0]);$tax=$db->table('taxes')->where('id',$db->insertID())->get()->getRow();}$db->table('item_fiscal_taxes')->insert(['item_fiscal_setting_id'=>$settingId,'tax_id'=>$tax->id,'sort_order'=>0,'is_active'=>1,'created_at'=>$now,'updated_at'=>$now]);
$sourceInvoice=$db->table('invoices')->where('deleted',0)->get(1)->getRowArray();unset($sourceInvoice['id']);$sourceInvoice=array_merge($sourceInvoice,['client_id'=>$client->id,'company_id'=>$company->id,'project_id'=>0,'status'=>'not_paid','tax_id'=>0,'tax_id2'=>0,'tax_id3'=>0,'discount_amount'=>'0','discount_total'=>'0','invoice_subtotal'=>'100','invoice_total'=>'100','tax'=>'0','tax2'=>'0','tax3'=>'0','deleted'=>0,'created_by'=>$user->id]);$db->table('invoices')->insert($sourceInvoice);$invoiceId=(int)$db->insertID();$sourceLine=$db->table('invoice_items')->where('deleted',0)->get(1)->getRowArray();unset($sourceLine['id']);$sourceLine=array_merge($sourceLine,['invoice_id'=>$invoiceId,'item_id'=>$itemId,'title'=>'Pricing isolated item','description'=>'Exact fiscal simulation fixture','quantity'=>'1','rate'=>'100','total'=>'100','taxable'=>0,'deleted'=>0]);$db->table('invoice_items')->insert($sourceLine);
$service=new App\Services\Fiscal\SaleTaxPricingSimulationService($db);$inclusive=$service->simulate($invoiceId,$issuerId,null,null,'tax_inclusive',$user->id,false);$a($inclusive['estimated_fiscal_total']==='100.00'&&$inclusive['difference_amount']==='0.00','Tax inclusive preserves total and difference zero.');$a($inclusive['estimated_fiscal_base']==='86.21'&&$inclusive['estimated_fiscal_tax_total']==='13.79','Tax inclusive extracts IVA 16 exactly to money precision.');$exclusive=$service->simulate($invoiceId,$issuerId,null,null,'tax_exclusive',$user->id,false);$a($exclusive['estimated_fiscal_base']==='100.00'&&$exclusive['estimated_fiscal_tax_total']==='16.00'&&$exclusive['estimated_fiscal_total']==='116.00','Tax exclusive adds IVA 16 to base 100.');$a($exclusive['difference_amount']==='16.00'&&$exclusive['estimated_balance']==='116.00'&&$exclusive['requires_confirmation']===1,'Tax exclusive reports difference, balance, and confirmation.');$preserve=$service->simulate($invoiceId,$issuerId,null,null,'preserve_total',$user->id,false);$a($preserve['estimated_fiscal_base']==='86.21'&&$preserve['estimated_fiscal_tax_total']==='13.79'&&$preserve['estimated_fiscal_total']==='100.00','Preserve total extracts base and tax without changing total.');
$repeat=$service->simulate($invoiceId,$issuerId,null,null,'tax_exclusive',$user->id,false);$a($repeat['estimated_fiscal_total']===$exclusive['estimated_fiscal_total']&&$repeat['calculation_payload']===$exclusive['calculation_payload'],'Repeated simulation is deterministic.');$db->table('invoice_payments')->insert(['invoice_id'=>$invoiceId,'payment_date'=>date('Y-m-d'),'payment_method_id'=>1,'amount'=>'100','note'=>'isolated pricing payment','created_by'=>$user->id,'created_at'=>$now,'deleted'=>0]);$paid=$service->simulate($invoiceId,$issuerId,null,null,'tax_exclusive',$user->id,false);$a($paid['previous_balance']==='0.00'&&$paid['estimated_balance']==='16.00','Paid sale gets estimated new balance 16 without modifying payment.');$a((string)$db->table('invoices')->where('id',$invoiceId)->get()->getRow()->invoice_total==='100','Simulation does not modify sale total.');
$persisted=$service->simulate($invoiceId,$issuerId,null,null,'tax_exclusive',$user->id,true);$a($persisted['id']>0&&$db->table('sale_fiscal_pricing_preparations')->where('id',$persisted['id'])->countAllResults()===1,'Simulation persists an immutable calculation snapshot.');$adjust=new App\Services\Fiscal\SaleTaxAdjustmentService($db);$result=$adjust->confirmAndApply((int)$persisted['id'],(int)$user->id,true);$a($result['new_total']==='116' || $result['new_total']==='116.00','Confirmed compatible adjustment updates sale through RISE totals.');$a((string)$db->table('invoice_payments')->where('invoice_id',$invoiceId)->get()->getRow()->amount==='100','Adjustment does not modify existing payment.');try{$adjust->confirmAndApply((int)$persisted['id'],(int)$user->id,true);$blocked=false;}catch(Throwable$e){$blocked=true;}$a($blocked,'Second application is idempotently blocked.');$a(!$db->tableExists('fiscal_documents'),'No fiscal document exists.');echo"\n$p passed, $f failed.\n";exit($f?1:0);
